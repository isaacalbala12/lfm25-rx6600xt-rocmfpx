# INVALID setup attempt

The first paired run stopped before model loading because
`ROCMFPX_PLUGIN_PATH` was not exported, so `ROCmFPXVulkan0` was unavailable.
It contains no benchmark sample and is excluded. The corrected complete run is
`work/results/v4-profile-c-chunk128-paired10-r2/`.
