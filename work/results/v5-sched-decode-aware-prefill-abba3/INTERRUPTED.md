The unlimited-idle candidate was stopped after the first complete pair met the
early-rejection condition. `pair-01-candidate/command.txt` was created before
the stop but contains no benchmark result and is excluded from `summary.json`.
The one valid pair is retained as exploratory evidence, not a confidence
interval.
